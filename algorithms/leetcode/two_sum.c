

int* twoSum(int* nums, int numSize, int target, int* returnSize) {
  
  for (int i = 0; i < numSize; i +=1) {
    if (m.contains(n)) {
      return [i, m.get(n)];
    }
      int diff = target - n;
      m.insert(diff);
    }
  
  return [-1, -1];
}
int main() {
  
}
